# vitruvio-sql

SQL over a Boltzmann brain: every module projected into a derived table, a read-only guard written on sqlglot,
and DuckDB to run what the guard admits.

Part of the [vitruvio](https://github.com/getsfumato/vitruvio) monorepo. See the repository root
for the architecture, the development loop, and the documentation.
